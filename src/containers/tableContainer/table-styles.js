import {makeStyles} from '@material-ui/core/styles'

const useStyles = makeStyles({
  table: {
    margin: '0 auto',
    minWidth: 1115,
  },
  container: {
    maxHeight: 450,
  }
});

export default useStyles;