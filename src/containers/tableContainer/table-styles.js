import {makeStyles} from '@material-ui/core/styles'

const useStyles = makeStyles({
  table: {
    margin: '0 auto',
    overflow: 'scroll',
    height: '55vh'
  },
  container: {
    margin: '0 auto',
    width: '100%',
    height: '55vh',
  },
  divider: {
    height: 1,
  }
});

export default useStyles;